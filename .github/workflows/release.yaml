name: release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      # Spelled out rather than left to the default so it stays in lockstep with the
      # appVersion the chart job stamps; a mismatch there is an ImagePullBackOff.
      # major.minor and latest are skipped for prereleases by the action itself.
      - id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern=v{{version}}
            type=semver,pattern=v{{major}}.{{minor}}
      - uses: docker/build-push-action@v7
        with:
          context: .
          build-args: version=${{ github.ref_name }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          sbom: true
          provenance: mode=max

  # One GitHub release per tag with the chart attached, the OCI chart, and for
  # stable versions the index on gh-pages, all off the same tag so they cannot
  # drift apart.
  release:
    runs-on: ubuntu-latest
    needs: image
    # Serialised because stable releases push to gh-pages.
    concurrency:
      group: release
      cancel-in-progress: false
    steps:
      # Full history also fetches gh-pages for the index update.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: azure/setup-helm@v5
      # The tag is the single source of truth. Without this the chart would ship
      # an appVersion, and therefore a default image tag, that was never pushed.
      - name: Stamp the chart with the tag
        run: |
          # Helm rejects a v prefix on version; appVersion must match the image tag
          # exactly (docker/metadata-action uses type=semver, which strips the v).
          version="${GITHUB_REF_NAME#v}"
          sed -i "s/^version: .*/version: ${version}/" charts/union-csi-driver/Chart.yaml
          sed -i "s/^appVersion: .*/appVersion: \"${version}\"/" charts/union-csi-driver/Chart.yaml
          helm lint charts/union-csi-driver
          helm package charts/union-csi-driver -d dist
      - name: Push chart to GHCR
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # OCI references must be lowercase, and the owner login is not.
          owner=$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')
          helm registry login ghcr.io -u "${GITHUB_ACTOR}" --password-stdin <<<"${GITHUB_TOKEN}"
          helm push dist/union-csi-driver-*.tgz "oci://ghcr.io/${owner}/charts"
      - name: Create the GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          prerelease=
          case "${GITHUB_REF_NAME}" in *-*) prerelease=--prerelease ;; esac
          gh release create "${GITHUB_REF_NAME}" dist/union-csi-driver-*.tgz --verify-tag --generate-notes ${prerelease}
      # Prereleases stay out of the index: it is what `helm repo add` resolves, and
      # it should list stable versions.
      - name: Add the chart to the Helm repository index
        if: ${{ !contains(github.ref_name, '-') }}
        run: |
          git config --global user.name "${GITHUB_ACTOR}"
          git config --global user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          git worktree add pages origin/gh-pages
          helm repo index dist --merge pages/index.yaml \
            --url "https://github.com/${GITHUB_REPOSITORY}/releases/download/${GITHUB_REF_NAME}"
          cp dist/index.yaml pages/index.yaml
          cd pages
          git add index.yaml
          git commit -m "Add union-csi-driver ${GITHUB_REF_NAME#v}"
          git push origin HEAD:gh-pages
